# Note: we include the spec file in the distribution tarball
#  (in addition to the .spec.in file) so that rpmbuild can work
#  directly on the tarball (e.g. rpmbuild -tb <tarball>).

EXTRA_DIST = $(PACKAGE).spec.in $(PACKAGE).spec
